---
- name: Create Volume Group
  lvg:
    pvs: "/dev/{{ block_device }}1"
    vg: "{{ lvm_vg_name }}"

- name: Create Logical Volume
  lvol:
    vg: "{{ lvm_vg_name }}"
    lv: "lv-mariadb"
    size: 100%FREE
    force: yes

- name: Format Logical Volume
  filesystem:
    dev: "/dev/{{ lvm_vg_name }}/lv-mariadb"  
    fstype: xfs
    force: yes
